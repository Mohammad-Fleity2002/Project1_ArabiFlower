# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

# trigger:
# - main

# pool:
#   vmImage: ubuntu-latest

# steps:
# - script: echo Hello, world!
#   displayName: 'Run a one-line script'

# - script: |
#     echo Add other tasks to build, test, and deploy your project.
#     echo See https://aka.ms/yaml
#   displayName: 'Run a multi-line script'

trigger:
- main  # trigger the pipeline on main update.

pool:
  vmImage: 'ubuntu-latest' 

steps:

- task: Docker@2
  displayName: 'Login to Docker Hub'
  inputs:
    command: login
    containerRegistry: 'ArabiFlower-Docker'  # Docker Hub service connection
    username: $(mohammadfleity2002)
    password: $(MohammadDockerPassword2002)


- script: |
    docker pull mohammadfleity2002/project1-arabiflower:latest
    docker run -d -p 8080:80 mohammadfleity2002/project1-arabiflower:latest
  displayName: 'Deploy to Docker'
